#!/bin/sh
cargo fmt --check || { printf '\n[hook] Formate d'\''abord avec : cargo fmt\n\n'; exit 1; }
cargo clippy --all-targets --all-features --locked -- -D warnings || {
    printf '\n[hook] Corrige les erreurs clippy avant de commiter.\n\n'
    exit 1
}
